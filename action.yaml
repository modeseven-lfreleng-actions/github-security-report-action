---
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2026 The Linux Foundation

name: "🔐 GitHub Security Report"
description: "Security and quality reporting across GitHub organisations"

inputs:
  scope:
    description: "Operating mode: auto | org | repo"
    required: false
    default: "auto"
  config:
    description: "JSON config (raw or base64), e.g. a vars/secrets entry"
    required: false
    default: ""
  org:
    description: "Single organisation (shorthand for org mode)"
    required: false
    default: ""
  repo:
    description: "owner/name for repo mode (defaults to this repository)"
    required: false
    default: ""
  token:
    description: "GitHub token (classic PAT for org mode; GITHUB_TOKEN for repo mode)"
    required: false
    default: ""
  token_env:
    # Deliberately empty by default. The tool now treats a supplied
    # --token-env as an override of every organisation's configured
    # token_env, so passing one unconditionally would flatten a multi-org
    # config whose organisations each read a different variable. Empty means
    # "say nothing", leaving each organisation on its own configured name;
    # the token is still exported under GITHUB_TOKEN in that case.
    description: "Environment variable name the token is exported under. When set it overrides the per-org token_env in your config; leave empty to let each organisation use its own"
    required: false
    default: ""
  output_dir:
    description: "Directory for GitHub Pages output (org mode)"
    required: false
    default: ""
  pages_url:
    description: "Published GitHub Pages URL (used in the Slack link)"
    required: false
    default: ""
  slack_channel:
    description: "Slack channel ID; overrides the config slack.channel (e.g. the SLACK_CHANNEL_ID variable)"
    required: false
    default: ""
  top_n:
    description: "Offenders shown per signal across all outputs (0 = no limit; default: config, else 10); per-output inputs below override it"
    required: false
    default: ""
  top_n_report:
    description: "Offenders shown per signal in the GitHub Pages output (0 = no limit; overrides top_n)"
    required: false
    default: ""
  top_n_cli:
    description: "Offenders shown per signal in the terminal output (0 = no limit; overrides top_n)"
    required: false
    default: ""
  top_n_slack:
    description: "Offenders shown per signal in the Slack digest (0 = no limit; overrides top_n)"
    required: false
    default: ""
  fail_threshold:
    description: "none | low | medium | high | critical | any (repo mode gate)"
    required: false
    default: "none"
  force_notify:
    description: "Post to Slack regardless of report_day"
    required: false
    default: "false"
  hide:
    # Space- or comma-separated category keys. Outranks the configuration and
    # is one-way: it can suppress a category the config would show, but cannot
    # re-enable one the config disabled.
    #
    # Deliberately empty by default. The personal review-queue table it would
    # most obviously name is already terminal-only, so no published surface
    # renders it and report.json omits it too (a category no published surface
    # carries is left out of that file). A non-empty default would also be
    # passed to whichever published version the runtime pin installs, which
    # fails with "No such option" on any release predating the flag.
    description: "Category keys to suppress on every output (space- or comma-separated; overrides config)"
    required: false
    default: ""
  graph_batch:
    # Deliberately empty by default, for the same reason as `hide`: a
    # non-empty default would be passed to whichever published version the
    # runtime pin installs, and fails with "No such option" on any release
    # predating the flag. A workflow that wants the size adjustable from
    # repository settings passes a variable here (e.g. vars.GSR_GRAPH_BATCH);
    # the `vars` context is not available inside a composite action, so the
    # fallback cannot live in this file.
    description: "Repositories per batched GraphQL prefetch query (minimum 1; default: config, else 10). Pass a repository or organisation variable such as vars.GSR_GRAPH_BATCH to adjust it from settings. A batch that still fails is halved automatically, so this sets the starting size"
    required: false
    default: ""
  tool_version:
    description: "Published PyPI version to run; ignored on pull_request or when use_local_source is true (both run from the checked-out source). Empty (the default) uses the Dependabot-managed pin in .github/runtime-pin/requirements.txt"
    required: false
    default: ""
  use_local_source:
    description: "Run from this checked-out source instead of the PyPI release (for testing unreleased code from any event)"
    required: false
    default: "false"

outputs:
  should_notify:
    description: "Whether today is a Slack notification day"
    value: ${{ steps.run.outputs.should_notify }}
  slack_payload:
    description: "Prebuilt Slack chat.postMessage payload (JSON)"
    value: ${{ steps.run.outputs.slack_payload }}
  failed:
    description: "Whether the repo-mode fail threshold was met"
    value: ${{ steps.run.outputs.failed }}

runs:
  using: "composite"
  steps:
    - name: "Set up uv"
      # yamllint disable-line rule:line-length
      uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d  # v10.0.1

    - name: "Run github-security-report"
      id: run
      shell: bash
      env:
        # Expose the token under the configured environment-variable name so
        # the tool resolves it by name (per-org token_env, default GITHUB_TOKEN).
        TOKEN_ENV: ${{ inputs.token_env || 'GITHUB_TOKEN' }}
        # The raw input, empty when the caller did not ask for a specific name.
        # Only a value that was actually supplied is passed through as an
        # override; see the token_env input above.
        INPUT_TOKEN_ENV: ${{ inputs.token_env }}
        # Input defaults are literal strings, so the github.token fallback is
        # applied here in env: where the expression is evaluated.
        TOKEN_VALUE: ${{ inputs.token || github.token }}
        # The explicitly-supplied token (empty when the caller relied on the
        # github.token fallback); used to fail fast for org mode below.
        INPUT_TOKEN: ${{ inputs.token }}
        INPUT_SCOPE: ${{ inputs.scope }}
        INPUT_CONFIG: ${{ inputs.config }}
        INPUT_ORG: ${{ inputs.org }}
        INPUT_REPO: ${{ inputs.repo }}
        INPUT_OUTPUT_DIR: ${{ inputs.output_dir }}
        INPUT_PAGES_URL: ${{ inputs.pages_url }}
        INPUT_SLACK_CHANNEL: ${{ inputs.slack_channel }}
        INPUT_TOP_N: ${{ inputs.top_n }}
        INPUT_TOP_N_REPORT: ${{ inputs.top_n_report }}
        INPUT_TOP_N_CLI: ${{ inputs.top_n_cli }}
        INPUT_TOP_N_SLACK: ${{ inputs.top_n_slack }}
        INPUT_FAIL_THRESHOLD: ${{ inputs.fail_threshold }}
        INPUT_FORCE_NOTIFY: ${{ inputs.force_notify }}
        INPUT_HIDE: ${{ inputs.hide }}
        INPUT_GRAPH_BATCH: ${{ inputs.graph_batch }}
        TOOL_VERSION: ${{ inputs.tool_version }}
        USE_LOCAL_SOURCE: ${{ inputs.use_local_source }}
        EVENT_NAME: ${{ github.event_name }}
        ACTION_PATH: ${{ github.action_path }}
      run: |
        # Run github-security-report
        # Validate token_env is a strict shell identifier before exporting,
        # so a malformed or crafted name cannot affect shell behaviour.
        if ! printf '%s' "$TOKEN_ENV" | grep -Eq '^[A-Za-z_][A-Za-z0-9_]*$'; then
          echo "Error: token_env '$TOKEN_ENV' is not a valid environment-variable name" >&2
          exit 1
        fi
        # Refuse to clobber reserved runner/Actions variables: exporting the
        # token into e.g. PATH or GITHUB_OUTPUT would break command resolution
        # or the tool's output/summary writing. GITHUB_TOKEN is the legitimate
        # default target and is intentionally not on the deny-list. -x anchors
        # the match to the whole name.
        # yamllint disable-line rule:line-length
        reserved='PATH|HOME|IFS|GITHUB_ENV|GITHUB_PATH|GITHUB_OUTPUT|GITHUB_STATE|GITHUB_STEP_SUMMARY|GITHUB_ACTION_PATH|GITHUB_WORKSPACE|RUNNER_TEMP|RUNNER_OS|RUNNER_ARCH|LD_PRELOAD|LD_LIBRARY_PATH'
        if printf '%s' "$TOKEN_ENV" | grep -Eqx "$reserved"; then
          echo "Error: token_env '$TOKEN_ENV' is a reserved variable name" >&2
          exit 1
        fi
        export "${TOKEN_ENV}=${TOKEN_VALUE}"

        # Org mode needs a classic PAT with org-level security scopes; the
        # ephemeral github.token is repo-scoped and 403s on org endpoints,
        # producing an empty/partial report. Fail fast at the action boundary
        # when org mode is selected without an explicit token, rather than
        # silently degrading. Explicit 'repo' scope is always repo mode (a
        # config there only overrides report settings), so it is exempt.
        if [ -z "$INPUT_TOKEN" ] && [ "$INPUT_SCOPE" != "repo" ] && \
            { [ "$INPUT_SCOPE" = "org" ] || [ -n "$INPUT_ORG" ] || \
              [ -n "$INPUT_CONFIG" ]; }; then
          echo "Error: org mode requires an explicit 'token' input (a classic" \
            "PAT with org security scopes); the default github.token is" \
            "repo-scoped and cannot read org-level security data." >&2
          exit 1
        fi

        # Hybrid runtime: pull requests (or an explicit use_local_source
        # toggle, for testing) exercise the checked-out source so unreleased
        # code runs; everything else runs a pinned PyPI release. An explicit
        # tool_version input wins; otherwise the pin is read from the
        # Dependabot-managed requirements file so version bumps arrive as
        # cooldown-gated PRs rather than a hand-edited default.
        if [ "$EVENT_NAME" = "pull_request" ] || [ "$USE_LOCAL_SOURCE" = "true" ]; then
          SOURCE="$ACTION_PATH"
        elif [ -n "$TOOL_VERSION" ]; then
          SOURCE="github-security-report==${TOOL_VERSION}"
        else
          PIN_FILE="$ACTION_PATH/.github/runtime-pin/requirements.txt"
          if [ ! -f "$PIN_FILE" ]; then
            echo "Error: runtime pin file not found: $PIN_FILE" >&2
            exit 1
          fi
          # First 'github-security-report==' pin token on its own line
          # (leading whitespace tolerated). sed captures up to the first
          # whitespace, '#', or ';', so inline comments and attached
          # environment markers are dropped without corrupting the token;
          # sed/head always exit 0, so this is pipefail-safe and the
          # emptiness check below reports a missing pin clearly.
          SOURCE="$(sed -n \
            's/^[[:space:]]*\(github-security-report==[^[:space:]#;]*\).*/\1/p' \
            "$PIN_FILE" | head -n1)"
          if [ -z "$SOURCE" ]; then
            echo "Error: no github-security-report pin found in $PIN_FILE" >&2
            exit 1
          fi
        fi

        args=(report --scope "$INPUT_SCOPE" --no-color)
        [ -n "$INPUT_TOKEN_ENV" ] && args+=(--token-env "$INPUT_TOKEN_ENV")
        [ -n "$INPUT_CONFIG" ] && args+=(--config-data "$INPUT_CONFIG")
        [ -n "$INPUT_ORG" ] && args+=(--org "$INPUT_ORG")
        [ -n "$INPUT_REPO" ] && args+=(--repo "$INPUT_REPO")
        [ -n "$INPUT_OUTPUT_DIR" ] && args+=(--output-dir "$INPUT_OUTPUT_DIR")
        [ -n "$INPUT_PAGES_URL" ] && args+=(--pages-url "$INPUT_PAGES_URL")
        [ -n "$INPUT_SLACK_CHANNEL" ] && args+=(--slack-channel "$INPUT_SLACK_CHANNEL")
        [ -n "$INPUT_TOP_N" ] && args+=(--top-n "$INPUT_TOP_N")
        [ -n "$INPUT_TOP_N_REPORT" ] && args+=(--top-n-report "$INPUT_TOP_N_REPORT")
        [ -n "$INPUT_TOP_N_CLI" ] && args+=(--top-n-cli "$INPUT_TOP_N_CLI")
        [ -n "$INPUT_TOP_N_SLACK" ] && args+=(--top-n-slack "$INPUT_TOP_N_SLACK")
        [ -n "$INPUT_FAIL_THRESHOLD" ] && args+=(--fail-threshold "$INPUT_FAIL_THRESHOLD")
        [ "$INPUT_FORCE_NOTIFY" = "true" ] && args+=(--force-notify)
        [ -n "$INPUT_GRAPH_BATCH" ] && args+=(--graph-batch "$INPUT_GRAPH_BATCH")
        # One --hide per category. Commas become spaces so either separator
        # works in a workflow, and the unquoted expansion is deliberate word
        # splitting; empty fields collapse, so a trailing separator cannot
        # produce a bare --hide with no value.
        if [ -n "$INPUT_HIDE" ]; then
          for category in $(printf '%s' "$INPUT_HIDE" | tr ',' ' '); do
            args+=(--hide "$category")
          done
        fi

        uvx --from "$SOURCE" github-security-report "${args[@]}"
